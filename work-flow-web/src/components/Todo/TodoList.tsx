import { useGetTodoByIdQuery } from "../../store/api/todoSlice";

function TodoList() {
  // const { data: todos, isFetching: isLoadingTodos } = useGetTodoByIdQuery();
  return <div>TodoList</div>;
}

export default TodoList;
